.env*
.DS_Store
node_modules/*

# Generated Tailwind output — built from input.css via `npm run build:css`
# (at deploy time, in CI, and locally). Not committed; see README.
/web/static/css/output.css
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Playwright authenticated storage
playwright/.auth/

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate*
terraform/terraform.tfvars
terraform/terraform.prod.tfvars

# Local working notes (not for the repo)
TODO.md
